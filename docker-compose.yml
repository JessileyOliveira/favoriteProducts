# Use postgres/example user/password credentials
version: '3.1'

services:
  db:
    image: postgres
    container_name: postgres
    restart: always
    environment:
      - POSTGRES_DB=favoriteproducts
      - POSTGRES_PASSWORD=docker
    ports:
      - 5432:5432
